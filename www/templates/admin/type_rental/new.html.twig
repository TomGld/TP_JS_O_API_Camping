{% extends 'base.html.twig' %}

{% block title %}New TypeRental{% endblock %}

{% block body %}
    <h1>Create new TypeRental</h1>

    {{ include('admin/type_rental/_form.html.twig') }}
    <div class="action-buttons">
        <div>
            <a href="{{path('app_type_rental_index')}}" class="btn btn-back">
                <i class="fas fa-arrow-left"></i>
                Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
